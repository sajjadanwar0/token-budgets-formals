# Verus Obligations — Token Budgets

Auto-generated by `gen_obligations.sh` on 2026-05-22 00:17:32 UTC
from `src/{lib,pool,concurrent}.rs` and the per-module Verus logs.

## Counts (verified)

| Module               | Obligations |
|----------------------|-------------|
| `src/lib.rs`         | 42            |
| `src/pool.rs`        | 11            |
| `src/concurrent.rs`  | 13            |
| **Total**            | **66**           |

Counts taken directly from `verus-{lib,pool,concurrent}.log`. Paper
claim is **66 obligations across three modules** (\S\ref{sec:not-verified}); the
`reproduce.sh` harness fails if this script's TOTAL is not 66.

## Reproduction

```bash
# From repository root:
cd token-budgets-formals/verus
verus src/lib.rs       2>&1 | tee verus-lib.log
verus src/pool.rs      2>&1 | tee verus-pool.log
verus src/concurrent.rs 2>&1 | tee verus-concurrent.log
bash gen_obligations.sh   # regenerates this file
```

Or, as a single step from the paper's reproduction harness:

```bash
bash reproduce.sh   # Verus step writes the three logs; gen runs after
```

## Index of obligation-bearing functions

The index below lists every function in the three modules carrying
at least one `requires` or `ensures` clause — the artefact-level
counterpart of "66 obligations". Note that Verus may discharge
multiple proof obligations per function (one per clause, plus
internal verification conditions), so the function count is a lower
bound on the obligation count by construction; the authoritative
total is the per-module log sum reported above.

For the exact predicates (the contents of each `requires`/`ensures`
clause), read the source files referenced below.


### `src/lib.rs` (42 obligations per `verus-lib.log`)

| Function | Kind | Clauses |
|----------|------|---------|
| `new` | fn | ensures |
| `spend` | fn | requires ensures |
| `split` | fn | requires ensures |
| `merge` | fn | requires ensures |
| `consume` | fn | requires ensures |
| `spend_with_receipt` | fn | requires ensures |
| `confirm` | fn | requires ensures |
| `apply_to` | fn | requires ensures |
| `trace_cap_soundness` | proof fn | requires ensures |
| `lemma_two_spend_conserves` | proof fn | requires ensures |
| `lemma_budget_after_spend_preserves` | proof fn | requires ensures |
| `end_to_end_cap_soundness` | proof fn | requires ensures |
| `try_spend` | fn | requires ensures |
| `try_spend_seq` | fn | requires ensures |

### `src/pool.rs` (11 obligations per `verus-pool.log`)

| Function | Kind | Clauses |
|----------|------|---------|
| `pool_cap_invariant` | proof fn | requires ensures |
| `pool_reserve_preserves_wf` | proof fn | requires ensures |
| `pool_release_preserves_wf` | proof fn | requires ensures |
| `pool_reserve_release_identity` | proof fn | requires ensures |
| `pool_reserve_monotone` | proof fn | requires ensures |
| `pool_trace_cap_sound` | proof fn | requires ensures |
| `pool_sums_nonneg` | proof fn | ensures |
| `pool_empty_trace_balanced` | proof fn | ensures |

### `src/concurrent.rs` (13 obligations per `verus-concurrent.log`)

| Function | Kind | Clauses |
|----------|------|---------|
| `lemma_seq_sum_nonneg` | proof fn | ensures |
| `lemma_element_leq_sum` | proof fn | requires ensures |
| `lemma_seq_sum_update` | proof fn | requires ensures |
| `lemma_seq_sum_push` | proof fn | ensures |
| `lemma_seq_sum_remove` | proof fn | requires ensures |
| `theorem_concurrent_spend_preserves` | proof fn | requires ensures |
| `theorem_concurrent_split_conserves` | proof fn | requires ensures |
| `theorem_concurrent_merge_conserves` | proof fn | requires ensures |
| `theorem_concurrent_op_preserves_wf` | proof fn | requires ensures |
| `theorem_concurrent_trace_cap_sound` | proof fn | requires ensures |
| `theorem_single_root_cap_sound` | proof fn | requires ensures |

## Trust assumptions

This per-module decomposition is contingent on Verus's standard
trust assumptions, documented in VerusBelt (Hance, Elbeheiry,
Matsushita, and Dreyer, PLDI 2026): ghost erasure, translation to
Z3, and a simplified type-layout model. Our Tier-4 verification
result inherits these assumptions. See the paper's
"What is not formally verified" section for the full list of
not-verified items.
